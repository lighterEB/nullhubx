# 工程进度交接（2026-03-17）

## 今日完成
1. CLI 最小闭环已接通 API：
- `install <component>` -> `POST /api/wizard/{component}`
- `start|stop|restart <component>/<name>` -> `POST /api/instances/{c}/{n}/{action}`
- `check-updates` -> `GET /api/updates`
- `update <component>/<name>` -> `POST /api/instances/{c}/{n}/update`
- `config <component>/<name>` -> `GET /api/instances/{c}/{n}/config`（`--edit` 暂提示回退只读）

2. 安装参数透传已支持：
- `--provider --api-key --model --memory --build-from-source`

3. 修复前端实例卡片 404：
- 新增实例详情路由 `/instances/[component]/[name]`
- 详情页已接入 Overview / Config / Logs / History / Memory / Skills 面板

4. 集成映射文档已扩展：
- 补充“阶段进展”
- 补充 Phase 1「实例内 Agent 管理 API 契约草案」（profiles + bindings）

## 当前代码状态
- 本地构建通过：
  - `zig build`
  - `cd ui && npm run build`
- 运行服务已停止，可明天按需重启。

## 待续工作（建议顺序）
1. 实现后端 API（Phase 1）
- `GET/PUT /api/instances/{c}/{n}/agents/profiles`
- `GET/PUT /api/instances/{c}/{n}/agents/bindings`

2. 服务端校验
- profile `id` 唯一
- `defaults.model_primary` 必须 `provider/model`
- binding 的 `agent_id` 需引用已存在 profile
- topic peer id 规范化为 `:thread:`

3. 前端落地
- 在实例详情页新增 Agent 管理入口（Profiles / Bindings）
- 保留 raw JSON 回退编辑

## 关键文件
- `src/main.zig`
- `src/cli.zig`
- `ui/src/routes/instances/[component]/[name]/+page.svelte`
- `docs/nullhubx-nullclaw-integration-mapping.md`

## 明天快速恢复
```bash
cd /home/huspc/projects/nullhubx
zig build
./zig-out/bin/nullhubx serve --port 19800 --no-open
```
