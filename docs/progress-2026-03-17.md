# 工程进度交接（2026-03-17）

## 本阶段已完成
1. 后端 Agent 管理 API 首版落地（Phase 1）
- `GET/PUT /api/instances/{c}/{n}/agents/profiles`
- `GET/PUT /api/instances/{c}/{n}/agents/bindings`
- 已接入 `src/server.zig` 路由与 `src/api/meta.zig` 路由目录
- 已完成基础校验：profile 唯一性、`defaults.model_primary` 格式、binding 的 `agent_id` 引用校验、legacy topic id 规范化

2. 配置合并能力增强（Phase 3 关键项）
- `PUT /agents/profiles` 支持按 `id` 合并并保留未知字段
- 兼容现有配置结构，减少全量覆盖导致的信息丢失

3. 稳定性修复（后端）
- 修复多处 JSON 生命周期/内存所有权问题（`toOwnedSlice`、arena 写入一致化）
- 修复日志路由遗漏、日志目录递归创建、`waitpid` 异常分支处理
- 修复若干测试环境路径与临时目录隔离问题

4. UI 统一与可用性收敛（Phase 2/4）
- 实例详情页补齐 Agents 面板与指标摘要
- 多个组件样式统一（卡片、状态、日志、向导、顶部导航等）
- 新增 `ui/src/lib/components/InstanceAgentsPanel.svelte`

## 当前验证结果
- `zig build` 通过
- `zig build test` 通过
- `cd ui && npm run build` 通过
- 运行态冒烟通过（2026-03-17）：
  - 启动：`./zig-out/bin/nullhubx serve --port 19810`
  - 脚本：`python3 tests/smoke_agents_api.py http://127.0.0.1:19810`
  - 覆盖：`/health`、`/api/status`、agents profiles/bindings 的 GET/PUT roundtrip、校验失败分支（400）、legacy topic id 规范化、自动恢复原配置

## 当前阶段状态
- Phase 0：done
- Phase 1：done
- Phase 2：done
- Phase 3：done
- Phase 4：done（UI 细节打磨与回归整理）

## 2026-03-18 收尾更新
1. 新增核心链路回归脚本：`tests/regression_core_flow.sh`
- 覆盖：`/health`、`/api/status`、`/api/components`、`/api/settings`、`/api/updates`、`/api/service/status`、404 分支
- 若存在实例，追加覆盖：`/config`、`/logs`，并自动调用 `tests/smoke_agents_api.py`
2. 新增回归执行手册：`docs/regression-checklist.md`
3. 本地实跑结果：`14 passed, 0 failed, 0 skipped`

## 关键文件
- `src/api/config.zig`
- `src/api/meta.zig`
- `src/api/wizard.zig`
- `src/server.zig`
- `src/supervisor/process.zig`
- `ui/src/routes/instances/[component]/[name]/+page.svelte`
- `ui/src/lib/components/InstanceAgentsPanel.svelte`
- `tests/smoke_agents_api.py`
- `docs/nullhubx-nullclaw-integration-mapping.md`

## 快速恢复
```bash
cd /home/huspc/projects/nullhubx
zig build
./zig-out/bin/nullhubx serve --port 19800
```
